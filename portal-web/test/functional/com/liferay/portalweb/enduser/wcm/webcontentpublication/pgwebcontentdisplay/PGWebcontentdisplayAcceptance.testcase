@component-name = "portal-wcm"
definition {
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "WCM";
	var pageName = "Web Content Display Page";
	var portletName = "Web Content Display";

	setUp {
		User.firstLoginPG();

		Page.addPG(pageName = "${pageName}");

		Portlet.addPG(pageName = "${pageName}", portletName = "${portletName}");
	}

	@priority = "5"
	test AddWebContent {
		var webContentContent = "WC WebContent Content";
		var webContentTitle = "WC WebContent Title";

		Page.gotoPG(pageName = "${pageName}");

		WebContent.addPGviaWCD(
			webContentContent = "${webContentContent}",
			webContentTitle = "${webContentTitle}"
		);
	}

	@priority = "5"
	test EditWebContent {
		var webContentContent = "WC WebContent Content";
		var webContentContentEdit = "WC WebContent Content Edit";
		var webContentTitle = "WC WebContent Title";
		var webContentTitleEdit = "WC WebContent Title Edit";

		Page.gotoPG(pageName = "${pageName}");

		WebContent.addPGviaWCD(
			webContentContent = "${webContentContent}",
			webContentTitle = "${webContentTitle}"
		);

		Page.gotoPG(pageName = "${pageName}");

		WebContent.editPGviaWCD(
			webContentContentEdit = "${webContentContentEdit}",
			webContentTitleEdit = "${webContentTitleEdit}"
		);
	}

	tearDown {
		WebContent.tearDownCP();

		Page.tearDownPG();
	}
}