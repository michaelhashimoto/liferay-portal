@default = "mouseMove"
definition {
	function mouseMove {
		selenium.mouseMove();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

	function mouseMoveAt {
		selenium.mouseMoveAt();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}