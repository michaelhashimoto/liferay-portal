@default = "isElementNotPresent"
definition {
	function isElementNotPresent {
		if (selenium.isElementPresent()) {
			selenium.isNotVisible();
		}
		else {
			selenium.isElementNotPresent();
		}
	}

	function isNotVisible {
		selenium.isNotVisible();
	}

	function pauseIsElementNotPresent {
		selenium.pause("1000");

		if (selenium.isElementPresent()) {
			selenium.isNotVisible();
		}
		else {
			selenium.isElementNotPresent();
		}
	}
}