@component-name = "portal-authentication"
definition {
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Authentication";

	setUp {
		User.firstLoginPG();
	}

	@priority = "5"
	test Login {
		User.logoutPG();

		User.loginPG();
	}
}