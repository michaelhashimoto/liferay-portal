<?xml version="1.0"?>

<project basedir="." name="portal-test-analytics-cloud" xmlns:antelope="antlib:ise.antelope.tasks">
	<import file="build-test.xml" />

	<macrodef name="build-analytics-cloud-docker-images">
		<sequential>
			<create-analytics-cloud-docker-compose
				docker.compose.build="true"
				docker.compose.file.name="docker-compose.yml"
			/>

			<local name="analytics.cloud.build" />

			<for list="${docker.compose.image.names}" param="docker.compose.image.name">
				<sequential>
					<if>
						<not>
							<available file="${project.dir}/@{docker.compose.image.name}.tar" />
						</not>
						<then>
							<property name="analytics.cloud.build" value="true" />
						</then>
					</if>
				</sequential>
			</for>

			<if>
				<equals arg1="${analytics.cloud.build}" arg2="true" />
				<then>
					<pull-docker-image-dependencies
						analytics.cloud.image.names.excludes="${analytics.cloud.asah.image.names.excludes}"
						analytics.cloud.repository.dir="${analytics.cloud.asah.dir}"
					/>

					<execute dir="${analytics.cloud.asah.dir}">
						./gradlew jar -Dliferay.ci=true

						./gradlew clean assemble -Dliferay.ci=true

						docker-compose --file=${docker.compose.file.path} --project-name=${analytics.cloud.project.name} build
					</execute>

					<pull-docker-image-dependencies
						analytics.cloud.image.names.excludes="${analytics.cloud.faro.image.names.excludes}"
						analytics.cloud.repository.dir="${analytics.cloud.faro.dir}"
					/>

					<local name="portal.gradlew" />

					<property location="${project.dir}/gradlew" name="portal.gradlew" />

					<execute dir="${analytics.cloud.faro.dir}">
						${portal.gradlew} buildAppDockerImage
					</execute>

					<execute>
						docker images
					</execute>

					<for list="${docker.compose.image.names}" param="docker.compose.image.name">
						<sequential>
							<execute dir="${project.dir}">
								mkdir -p $(dirname @{docker.compose.image.name}.tar)

								docker save -o @{docker.compose.image.name}.tar @{docker.compose.image.name}:latest
							</execute>
						</sequential>
					</for>
				</then>
				<else>
					<for list="${docker.compose.image.names}" param="docker.compose.image.name">
						<sequential>
							<execute dir="${project.dir}">
								docker load -i @{docker.compose.image.name}.tar
							</execute>
						</sequential>
					</for>
				</else>
			</if>
		</sequential>
	</macrodef>

	<macrodef name="create-analytics-cloud-docker-compose">
		<attribute default="false" name="docker.compose.build" />
		<attribute name="docker.compose.file.name" />

		<sequential>
			<fail message="Please set the property ${env.DOCKER_ENABLED}." unless="env.DOCKER_ENABLED" />

			<var name="docker.compose.file.path" unset="true" />

			<property name="docker.compose.file.path" value="${analytics.cloud.asah.dir}/docker-compose.temp.yml" />

			<delete file="${docker.compose.file.path}" />

			<get-analytics-cloud-faro-image-name />

			<beanshell>
				<![CDATA[
					import com.liferay.jenkins.results.parser.JenkinsResultsParserUtil;

					import java.util.regex.Matcher;
					import java.util.regex.Pattern;

					import org.yaml.snakeyaml.Yaml;

					Pattern hostnamePattern = Pattern.compile("(cloud-[^\\.]+|test-\\d+-\\d+-\\d+)(\\.lax\\.liferay\\.com)?");

					public Map getNetworksMap() {
						String hostname = project.getProperty("env.HOSTNAME");

						if (JenkinsResultsParserUtil.isNullOrEmpty(hostname)) {
							return null;
						}

						Matcher hostnameMatcher = hostnamePattern.matcher(hostname);

						if (!hostnameMatcher.find()) {
							return null;
						}

						Map externalMap = new HashMap();

						externalMap.put("name", hostnameMatcher.group(1));

						Map defaultMap = new HashMap();

						defaultMap.put("external", externalMap);

						Map networksMap = new HashMap();

						networksMap.put("default", defaultMap);

						return networksMap;
					}

					System.out.println("docker.compose.build=@{docker.compose.build}");

					Yaml yaml = new Yaml();

					Map map = yaml.load(new FileInputStream(new File(project.getProperty("analytics.cloud.asah.dir"), "@{docker.compose.file.name}")));

					List dockerComposeImageNames = new ArrayList();

					for (String key : map.keySet()) {
						if (!key.equals("services")) {
							continue;
						}

						Object servicesMapObject = map.get(key);

						if (!(servicesMapObject instanceof Map)) {
							continue;
						}

						Map servicesMap = (Map)servicesMapObject;

						for (String serviceName : servicesMap.keySet()) {
							Object serviceObject = servicesMap.get(serviceName);

							if (!(serviceObject instanceof Map)) {
								continue;
							}

							Map serviceMap = (Map)serviceObject;

							String dockerComposeImageName = "com-liferay-osb-asah-private-" + serviceName;

							if (serviceMap.containsKey("build")) {
								dockerComposeImageNames.add(dockerComposeImageName);
							}

							if (!Objects.equals("@{docker.compose.build}", "true")) {
								serviceMap.remove("build");

								if (!serviceMap.containsKey("image")) {
									serviceMap.put("image", dockerComposeImageName + ":latest");
								}
							}

							List ports = new ArrayList();

							Object portsObject = serviceMap.get("ports");

							if (portsObject instanceof List) {
								ports.addAll((List)portsObject);
							}

							if (serviceName.equals("osbasahupgrade")) {
								ports.add(project.getProperty("analytics.cloud.asah.upgrade.port") + ":8080");

								serviceMap.put("image", project.getProperty("analytics.cloud.faro.image.name") + ":latest");

								dockerComposeImageNames.add(project.getProperty("analytics.cloud.faro.image.name"));
							}
							else if (serviceName.equals("osbfarofrontend")) {
								ports.remove("8080:8080");
								ports.remove("11311:11311");

								ports.add(project.getProperty("analytics.cloud.faro.dxp.port") + ":8080");
								ports.add(project.getProperty("analytics.cloud.faro.osgi.console.port") + ":11311");
							}
							else if (serviceName.equals("test_elasticsearch")) {
								ports.remove("9200:9200");

								ports.add(project.getProperty("analytics.cloud.asah.elasticsearch.port") + ":9200");
							}

							serviceMap.put("ports", ports);
						}
					}

					Map networksMap = getNetworksMap();

					if (networksMap != null) {
						map.put("networks", networksMap);
					}

					File dockerComposeFile = new File(project.getProperty("docker.compose.file.path"));

					PrintWriter printWriter = new PrintWriter(dockerComposeFile);

					yaml.dump(map, printWriter);

					project.setProperty("docker.compose.file.path", JenkinsResultsParserUtil.getCanonicalPath(dockerComposeFile));
					project.setProperty("docker.compose.image.names", JenkinsResultsParserUtil.join(",", dockerComposeImageNames));
				]]>
			</beanshell>

			<print-file file.name="${docker.compose.file.path}" />
		</sequential>
	</macrodef>

	<macrodef name="get-analytics-cloud-faro-image-name">
		<sequential>
			<var name="analytics.cloud.faro.image.name" unset="true" />

			<beanshell>
				<![CDATA[
					import com.liferay.jenkins.results.parser.JenkinsResultsParserUtil;

					import java.util.regex.Matcher;
					import java.util.regex.Pattern;

					import org.yaml.snakeyaml.Yaml;

					Pattern dockerImageUserPattern = Pattern.compile("def dockerImageUser = \"([^\"]+)\"");
					Pattern dockerImageIdPattern = Pattern.compile("def dockerImageId = \"([^\"]+)\"");

					public String getFaroImageName() {
						File faroBuildGradleFile = new File(project.getProperty("analytics.cloud.faro.dir"), "build-ext.gradle");

						if (!faroBuildGradleFile.exists()) {
							faroBuildGradleFile = new File(project.getProperty("analytics.cloud.faro.dir"), "build.gradle");
						}

						String content = JenkinsResultsParserUtil.read(faroBuildGradleFile);

						String dockerImageUser = null;

						Matcher dockerImageUserMatcher = dockerImageUserPattern.matcher(content);

						if (dockerImageUserMatcher.find()) {
							dockerImageUser = dockerImageUserMatcher.group(1);
						}

						String dockerImageId = null;

						Matcher dockerImageIdMatcher = dockerImageIdPattern.matcher(content);

						if (dockerImageIdMatcher.find()) {
							dockerImageId = dockerImageIdMatcher.group(1);
						}

						if (JenkinsResultsParserUtil.isNullOrEmpty(dockerImageUser)) {
							return dockerImageId;
						}

						return dockerImageUser + "/" + dockerImageId;
					}

					project.setProperty("analytics.cloud.faro.image.name", getFaroImageName());
				]]>
			</beanshell>
		</sequential>
	</macrodef>

	<macrodef name="print-analytics-cloud-logs">
		<sequential>
			<create-analytics-cloud-docker-compose docker.compose.file.name="docker-compose.yml" />

			<local name="container.names" />

			<beanshell>
				<![CDATA[
					import com.liferay.jenkins.results.parser.JenkinsResultsParserUtil;

					import java.util.regex.Matcher;
					import java.util.regex.Pattern;

					public static List getContainerNames() {
						File dockerComposeFile = new File(project.getProperty("docker.compose.file.path"));

						if (!dockerComposeFile.exists()) {
							return new ArrayList();
						}

						List containerNames = new ArrayList();

						String content = JenkinsResultsParserUtil.read(dockerComposeFile);

						Pattern pattern = Pattern.compile("container_name: (.*)");

						Matcher matcher = pattern.matcher(content);

						while (matcher.find()) {
							containerNames.add(matcher.group(1));
						}

						return containerNames;
					}

					project.setProperty("container.names", JenkinsResultsParserUtil.join(",", getContainerNames()))
				]]>
			</beanshell>

			<local name="docker.logs.dir" />

			<condition else="/tmp/docker-logs" property="docker.logs.dir" value="${env.BUILD_DIR}/docker-logs">
				<and>
					<isset property="env.BUILD_DIR" />
					<isset property="env.BUILD_USER_CONTENT_URL" />
				</and>
			</condition>

			<mkdir dir="${docker.logs.dir}" />

			<for list="${container.names}" param="container.name">
				<sequential>
					<local name="container.file.name" />

					<beanshell>
						<![CDATA[
							public int getRetryCount() {
								String retryCount = project.getProperty("analytics.cloud.retry.count");

								if (retryCount.matches("\\d+")) {
									return Integer.valueOf(retryCount);
								}

								return 3;
							}

							for (int i = 0; i < getRetryCount(); i++) {
								String containerFileName = "@{container.name}.log";

								if (i > 0) {
									containerFileName = "@{container.name}." + i + ".log";
								}

								File containerFile = new File(project.getProperty("docker.logs.dir"), containerFileName);

								if (containerFile.exists()) {
									continue;
								}

								project.setProperty("container.file.name", containerFileName);

								break;
							}
						]]>
					</beanshell>

					<execute failonerror="false">
						<![CDATA[
							docker logs @{container.name} > ${docker.logs.dir}/${container.file.name}
						]]>
					</execute>

					<if>
						<isset property="env.BUILD_USER_CONTENT_URL" />
						<then>
							<echo>${env.BUILD_USER_CONTENT_URL}/docker-logs/${container.file.name}</echo>
						</then>
						<else>
							<print-file file.name="${docker.logs.dir}/${container.file.name}" />
						</else>
					</if>
				</sequential>
			</for>
		</sequential>
	</macrodef>

	<macrodef name="pull-docker-image-dependencies">
		<attribute default="" name="analytics.cloud.image.names.excludes" />
		<attribute name="analytics.cloud.repository.dir" />

		<sequential>
			<local name="analytics.cloud.image.names.excludes" />

			<property name="analytics.cloud.image.names.excludes" value="@{analytics.cloud.image.names.excludes}" />

			<beanshell>
				<![CDATA[
					import com.liferay.jenkins.results.parser.JenkinsResultsParserUtil;

					String excludedImageNames = project.getProperty("analytics.cloud.image.names.excludes");

					JenkinsResultsParserUtil.pullDockerImageDependencies(new File("@{analytics.cloud.repository.dir}"), excludedImageNames.split(","));
				]]>
			</beanshell>
		</sequential>
	</macrodef>

	<macrodef name="start-analytics-cloud">
		<sequential>
			<fail message="Please set the property ${env.DOCKER_ENABLED}." unless="env.DOCKER_ENABLED" />

			<build-analytics-cloud-docker-images />

			<create-analytics-cloud-docker-compose docker.compose.file.name="docker-compose.yml" />

			<replaceregexp
				file="${analytics.cloud.faro.dir}/osb-faro-docker/local/Dockerfile"
				match="FARO_URL=http://localhost:8080"
				replace="FARO_URL=${analytics.cloud.faro.frontend.url}"
			/>

			<echo file="${analytics.cloud.asah.dir}/local.env">OSB_ASAH_BACKEND_URL=${analytics.cloud.asah.backend.url}
OSB_ASAH_POSTGRESQL_ENABLED=false
OSB_ASAH_PUBLISHER_URL=${analytics.cloud.asah.publisher.url}
OSB_FARO_FRONTEND_URL=${analytics.cloud.faro.frontend.url}</echo>

			<property location="${analytics.cloud.asah.dir}/start_analytics_cloud.sh" name="analytics.cloud.start.sh" />

			<echo file="${analytics.cloud.start.sh}">
				#!/bin/bash

				touch local.env

				docker-compose --file=${docker.compose.file.path} --project-name=${analytics.cloud.project.name} up --detach

				while [[ -n $(docker ps | grep "health: starting") ]] || [[ -n $(docker ps | grep "unhealthy") ]]
				do
					echo "Waiting for docker compose to be healthy."

					sleep 30
				done

				while [[ -z $(docker logs faro | grep "Completed demo data creation") ]]
				do
					echo "Waiting for faro to startup."

					sleep 15
				done

				docker logs faro

				docker container prune -f
				docker image prune -f
				docker volume prune -f
			</echo>

			<trycatch>
				<try>
					<retry retrycount="${analytics.cloud.retry.count}" retrydelay="30000">
						<sequential>
							<trycatch property="analytics.cloud.error">
								<try>
									<local name="analytics.cloud.start.result" />

									<exec dir="${analytics.cloud.asah.dir}" executable="/bin/bash" resultproperty="analytics.cloud.start.result" timeout="${analytics.cloud.start.timeout}">
										<arg value="${analytics.cloud.start.sh}" />
									</exec>

									<if>
										<not>
											<equals arg1="${analytics.cloud.start.result}" arg2="0" />
										</not>
										<then>
											<fail message="Failed to start Analytics Cloud with error code ${analytics.cloud.start.result}." />
										</then>
									</if>
								</try>
								<catch>
									<print-analytics-cloud-logs />

									<stop-analytics-cloud />

									<fail message="${analytics.cloud.error}" />
								</catch>
							</trycatch>
						</sequential>
					</retry>
				</try>
				<catch>
					<echo>Failed to create Analytics Cloud demo data.</echo>

					<fail message="${analytics.cloud.error}" />
				</catch>
			</trycatch>

			<delete file="${analytics.cloud.start.sh}" />
		</sequential>
	</macrodef>

	<macrodef name="start-analytics-cloud-integration">
		<sequential>
			<fail message="Please set the property ${env.DOCKER_ENABLED}." unless="env.DOCKER_ENABLED" />

			<create-analytics-cloud-docker-compose docker.compose.file.name="docker-compose.integration-test.yml" />

			<pull-docker-image-dependencies
				analytics.cloud.image.names.excludes="${analytics.cloud.asah.image.names.excludes}"
				analytics.cloud.repository.dir="${analytics.cloud.asah.dir}"
			/>

			<execute dir="${analytics.cloud.asah.dir}">
				docker-compose --file=${docker.compose.file.path} up -d

				docker ps
			</execute>

			<exec executable="/bin/bash" outputproperty="network.ip.address">
				<arg value="-c" />
				<arg value="docker inspect ${short.hostname} -f '{{range .NetworkSettings.Networks}}{{.Gateway}}{{end}}'" />
			</exec>

			<echo file="${analytics.cloud.asah.dir}/temp-build-ext.gradle">import org.apache.tools.ant.taskdefs.condition.Os

subprojects {
	tasks.withType(Test) {
		doFirst {
			environment "LCP_ENGINE_ELASTICSEARCH_SERVER_IP", "${network.ip.address}"
			environment "LCP_PROJECT_ID", "test"
			environment "POSTGRESQL_SERVER_IP", "test_postgresql"
			environment "OSB_ASAH_BIG_QUERY_URL", "http://${network.ip.address}:9050"
			environment "OSB_ASAH_PUBSUB_EMULATOR_URL", "http://${network.ip.address}:8095"
			environment "OSB_ASAH_REDIS_URL", "http://${network.ip.address}:6379"
			environment.remove("GOOGLE_APPLICATION_CREDENTIALS")
		}

		maxHeapSize = "4096m"
	}
}

if (Os.isFamily(Os.FAMILY_WINDOWS)) {
	subprojects {
		afterEvaluate {
			project.tasks.withType(Test) {
				environment "DOCKER_COMPOSE_LOCATION", "C:/Program Files/Docker/Docker/resources/bin/docker-compose.exe"
				environment "DOCKER_LOCATION", "C:/Program Files/Docker/Docker/resources/bin/docker.exe"
			}
		}
	}
}</echo>

			<copy
				file="${analytics.cloud.asah.dir}/temp-build-ext.gradle"
				tofile="${analytics.cloud.asah.dir}/build-ext.gradle"
			/>

			<execute dir="${analytics.cloud.asah.dir}">
				docker ps

				docker network ls

				docker network inspect ${short.hostname}

				cat build-ext.gradle

				./gradlew testIntegration
			</execute>
		</sequential>
	</macrodef>

	<macrodef name="stop-analytics-cloud">
		<sequential>
			<fail message="Please set the property ${env.DOCKER_ENABLED}." unless="env.DOCKER_ENABLED" />

			<create-analytics-cloud-docker-compose docker.compose.file.name="docker-compose.yml" />

			<execute dir="${analytics.cloud.asah.dir}">
				docker ps -a

				touch local.env

				docker-compose --file=${docker.compose.file.path} --project-name=${analytics.cloud.project.name} down --volumes

				docker container prune -f
				docker image prune -f
				docker volume prune -f
			</execute>
		</sequential>
	</macrodef>

	<macrodef name="stop-analytics-cloud-integration">
		<sequential>
			<fail message="Please set the property ${env.DOCKER_ENABLED}." unless="env.DOCKER_ENABLED" />

			<create-analytics-cloud-docker-compose docker.compose.file.name="docker-compose.integration-test.yml" />

			<execute dir="${analytics.cloud.asah.dir}">
				docker ps -a

				docker-compose --file=${docker.compose.file.path} down --volumes

				docker container prune -f
				docker image prune -f
				docker volume prune -f
			</execute>
		</sequential>
	</macrodef>

	<target name="start-analytics-cloud">
		<stop-analytics-cloud />

		<start-analytics-cloud />
	</target>

	<target name="start-analytics-cloud-integration">
		<stop-analytics-cloud-integration />

		<start-analytics-cloud-integration />
	</target>

	<target name="stop-analytics-cloud">
		<print-analytics-cloud-logs />

		<stop-analytics-cloud />
	</target>

	<target name="stop-analytics-cloud-integration">
		<print-analytics-cloud-logs />

		<stop-analytics-cloud-integration />
	</target>
</project>