<?xml version="1.0"?>

<project basedir="." name="portal-test-saml" xmlns:antelope="antlib:ise.antelope.tasks">
	<import file="build-test.xml" />

	<target name="prepare-oauth2-osgi-configuration">
		<property name="osgi.dependency.dir" value="${project.dir}/portal-web/test/functional/com/liferay/portalweb/tests/coreinfrastructure/security/oauth2/dependencies" />

		<copy
			file="${osgi.dependency.dir}/${osgi.config.file.name}"
			todir="${liferay.home}"
		/>

		<replace
			file="${liferay.home}/${osgi.config.file.name}"
			token="companyId=L&quot;20098&quot;"
			value="companyId=L&quot;${companyId}&quot;"
		/>

		<move
			file="${liferay.home}/${osgi.config.file.name}"
			todir="${liferay.home}/osgi/configs"
		/>
	</target>

	<target name="prepare-oauth2-react-application">
		<property location="${project.dir}/workspaces/liferay-oauth2-workspace" name="liferay.oauth2.workspace" />

		<get-host-git-dir />

		<propertyregex
			input="${project.dir}"
			property="portal.dir.name"
			regexp=".*\/(.*)"
			select="\1"
		/>

		<start-docker-container image="liferay/liferay-ci:node-19_1.0.0" name="${env.HOSTNAME}_node">\
			--expose 3000 \
			--mount type=bind,source=${host.git.dir}/${portal.dir.name}/workspaces/liferay-oauth2-workspace,target=/root/liferay-oauth2-workspace \
		</start-docker-container>

		<docker-execute container="${env.HOSTNAME}_node">
			<![CDATA[
				cd /root/liferay-oauth2-workspace/client-extensions/liferay-oauth2-etc

				yarn install

				(yarn start &)
			]]>
		</docker-execute>
	</target>
</project>